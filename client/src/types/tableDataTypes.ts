import type { ReactNode } from "react"

export type  StudentTableDetailsType={
    id:string,
    content:(number|string|ReactNode)[],
}

export type  MentorTableDetailsType={
    id:string,
    content:(number|string|ReactNode)[],
}


export type TableDetailsType={
     id:string,
    content:(number|string|ReactNode)[],
}
